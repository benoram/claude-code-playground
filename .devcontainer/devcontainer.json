{
  "name": "Claude Code Playground",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "VARIANT": "jammy"
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/aws-cli:1": {},
    "ghcr.io/devcontainers/features/go:1": {}
  },
  // Create ~/.aws directory on host if it doesn't exist (required for bind mount)
  "initializeCommand": "mkdir -p ~/.aws",
  "postStartCommand": ".devcontainer/setup-aws-credentials.sh",
  "remoteUser": "vscode",
  "mounts": [
    // Mount host AWS credentials for local development (read-only)
    // initializeCommand ensures ~/.aws exists; in Codespaces this will be empty
    "source=${localEnv:HOME}${localEnv:USERPROFILE}/.aws,target=/home/vscode/.aws-host,type=bind,readonly"
  ],
  "containerEnv": {
    // Pass through local AWS profile if set
    "AWS_PROFILE_LOCAL": "${localEnv:AWS_PROFILE}",
    "AWS_REGION": "${localEnv:AWS_REGION}",
    "AWS_DEFAULT_REGION": "${localEnv:AWS_DEFAULT_REGION}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "amazonwebservices.aws-toolkit-vscode"
      ]
    }
  },
  // Claude Code Pro: Run 'claude login' after container starts to authenticate via OAuth
  "postCreateCommand": "echo '\n📋 Claude Code installed! Run: claude login\n'"
}
